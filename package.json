{
  "name": "@wormhole-foundation/wormhole-monitor",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc -b -i",
    "clean": "tsc -b --clean",
    "watch": "tsc -b -w -i"
  },
  "workspaces": [
    "common",
    "watcher",
    "dashboard",
    "cloud_functions",
    "database"
  ],
  "dependencies": {
    "@wormhole-foundation/sdk-base": "^0.5.1",
    "@wormhole-foundation/sdk-cosmwasm": "^0.5.1",
    "@wormhole-foundation/sdk-evm": "^0.5.1"
  },
  "devDependencies": {
    "prettier": "^2.8.1",
    "typescript": "^5.4.3"
  }
}
